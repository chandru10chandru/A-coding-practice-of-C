//Write a program to print your name and age.
#include <stdio.h>
int main() 
{
    int age = 19;
    char c[] = "chandru";// if we want add string to cahr use [] this brackets other it gives you error
    printf("the name is %s and age is %d",c,age); //use %s for string
return 0;
}